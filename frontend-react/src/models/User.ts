export type UserProfileToken = {
    userID: number;
    username: string;
    email: string;
    token: string;
};

export type UserProfile = {
    userID: number;
    username: string;
    email: string;
};
